services:
  cafofo-bring:
    image: ghcr.io/leandro-rocha/cafofo-bring:latest
    container_name: cafofo-bring
    restart: unless-stopped
    environment:
      - PORT=3001
      - DB_PATH=/data/shopping.db
      - WA_DATA_DIR=/data
      - ZAP_URL=http://192.168.0.220:3010
      - SELF_URL=http://192.168.0.220:4001
    volumes:
      - db-data:/data
    ports:
      - "4001:3001"

volumes:
  db-data:
